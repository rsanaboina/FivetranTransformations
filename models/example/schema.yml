
version: 2

models:
  - name: my_first_dbt_model
    description: "A starter dbt model"
    columns:
      - name: email_address
        description: "The primary key for this table"
        tests:
          - unique
          - not_null

  - name: my_second_dbt_model
    description: "A starter dbt model"
    columns:
      - name: email_address
        description: "The primary key for this table"
        tests:
          - unique
          - not_null
  
  - name: update_city_model
    description: "A starter dbt model"
    columns:
      - name: email_address
        description: "The primary key for this table"
        tests:
          - unique
          - not_null
  
  - name: update_states_model
    description: "A starter dbt model"
    columns:
      - name: email_address
        description: "The primary key for this table"
        tests:
          - unique
          - not_null